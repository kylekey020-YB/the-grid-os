import { Card, CardContent } from "@/components/ui/card";
import { StatusBadge } from "@/components/StatusBadge";
export type TimelineItem = { title: string; description: string; status?: string; };
export function Timeline({ items }: { items: TimelineItem[] }) { return <div className="space-y-3">{items.map((item, index) => <Card key={item.title} className="overflow-hidden"><CardContent className="relative flex gap-4 p-4"><div className="flex flex-col items-center"><span className="flex h-8 w-8 items-center justify-center rounded-full border border-primary/30 bg-primary/10 text-xs text-primary">{index + 1}</span>{index < items.length - 1 ? <span className="mt-2 h-full min-h-8 w-px bg-border" /> : null}</div><div className="min-w-0 flex-1 space-y-2"><div className="flex flex-wrap items-center gap-2"><h3 className="font-display text-base font-semibold">{item.title}</h3>{item.status ? <StatusBadge label={item.status} tone="manual" /> : null}</div><p className="text-sm leading-6 text-muted-foreground">{item.description}</p></div></CardContent></Card>)}</div>; }
