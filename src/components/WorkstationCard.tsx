import { ArrowUpRight } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { StatusBadge } from "@/components/StatusBadge";
export type Workstation = { name: string; purpose: string; stage: string; lastUpdated: string; status: string; tone?: "default" | "manual" | "success" | "danger" | "muted" | "beta"; };
export function WorkstationCard({ workstation }: { workstation: Workstation }) { return <Card className="group overflow-hidden transition duration-300 hover:border-primary/30 hover:shadow-glow"><CardHeader className="flex-row items-start justify-between gap-4"><div className="space-y-2"><StatusBadge label={workstation.status} tone={workstation.tone ?? "manual"} /><CardTitle>{workstation.name}</CardTitle></div><ArrowUpRight className="h-4 w-4 text-muted-foreground transition group-hover:text-primary" /></CardHeader><CardContent className="space-y-4"><p className="text-sm leading-6 text-muted-foreground">{workstation.purpose}</p><div className="grid grid-cols-2 gap-3 text-xs"><div className="rounded-md border border-border/70 bg-background/50 p-3"><p className="uppercase tracking-[0.14em] text-muted-foreground">Stage</p><p className="mt-1 font-medium text-foreground">{workstation.stage}</p></div><div className="rounded-md border border-border/70 bg-background/50 p-3"><p className="uppercase tracking-[0.14em] text-muted-foreground">Updated</p><p className="mt-1 font-medium text-foreground">{workstation.lastUpdated}</p></div></div></CardContent></Card>; }
